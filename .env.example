DATABASE_URL=postgres://coral:coral@localhost:5433/coral
PORT=8787

# Optional. Metadata search needs no key; full text does.
# Without it retrieval stops at the abstract and students upload papers instead.
# Free from https://openalex.org/users
OPENALEX_API_KEY=
